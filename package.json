{
  "name": "leverage-app",
  "version": "2.0.0",
  "private": true,
  "description": "Leverage — Levels 2-4 time management training app. Pure client-side React app, no backend.",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
