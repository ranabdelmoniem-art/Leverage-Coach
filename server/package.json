{
  "name": "leverage-coach-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal backend proxy for the Leverage AI time coach — holds the Anthropic API key server-side.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
