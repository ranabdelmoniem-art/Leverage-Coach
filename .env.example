# Copy this file to .env (for local dev with the Express server) and fill in
# a real key. Never commit the real .env file.
# On Vercel/Render/Railway/etc, set this as an environment variable in the
# platform's dashboard instead of a file.

ANTHROPIC_API_KEY=sk-ant-...
